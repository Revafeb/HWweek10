const config = {
    development: {
        username: "postgres",
        password: "postgres",
        database: "HWweek10",
        host: "localhost",
        dialect: "postgres"
    }
}

module.exports = config;